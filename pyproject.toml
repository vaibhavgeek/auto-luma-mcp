[project]
name = "auto-luma-mcp-agents"
version = "0.1.0"
description = "Workspace metadata for the LumaBot hackathon repository."
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["shared"]
